/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BookVacationComponent } from './book-vacation.component';

describe('BookVacationComponent', () => {
  let component: BookVacationComponent;
  let fixture: ComponentFixture<BookVacationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [BookVacationComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(BookVacationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */